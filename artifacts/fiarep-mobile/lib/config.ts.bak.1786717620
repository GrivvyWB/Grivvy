// Google Maps Platform API key — used for Geocoding + Solar APIs.
// Restrict this key to Geocoding API and Solar API in the Google Cloud console,
// and set a daily quota cap so a leaked key can't run up a bill.
//
// For a public release, move these calls behind your own backend (e.g. Replit)
// so the key never ships in the app, and point the fetch URLs at your proxy.
export const GOOGLE_API_KEY = 'AIzaSyB0TpBsnyUpRnBRu2zJj6ITdKX_KURp-PM';
